# Example YAML configuration file

database:
  host: localhost
  port: 5432
  username: admin
  password: secret

server:
  host: 0.0.0.0
  port: 8080
  log_level: debug

features:
  - authentication
  - caching
  - logging

